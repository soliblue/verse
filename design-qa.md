# Citrus design verification

Source visual truth: selected second generated mockup, copied to `plans/assets/citrus-reference.png`.

Intentional changes: no slogan, cream primary controls instead of green, native controls and system typography, existing recording behavior preserved.

Implementation screenshot: unavailable.

Viewport and state: iPhone portrait hub and 240-point keyboard, idle and recording states require capture.

Fonts, spacing, colors, image quality, and copy: code prepared, rendered comparison pending. No visual acceptance claimed.

Full-view and focused comparison evidence: unavailable because the available simulator tool fails with `spawn xcrun ENOENT` on this Linux host.

Comparison history: no rendered iteration yet.

Next checks: build the native targets through GitHub CI, inspect hub and keyboard screenshots against the reference, test recording, insertion, imports, history, settings, and accessibility sizes. Correct any major or moderate visual differences before release.

final result: blocked
