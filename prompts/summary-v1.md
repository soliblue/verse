For every candidate, return `normalized_item_id`, `title`, `summary`, `body`, `why_selected`, and `kind`. The summary should state the signal in one or two sentences. The body should explain the useful details in two to four short paragraphs. The selection reason should connect the item to the supplied interests. Use `paper`, `event`, or `technique` for kind. Do not return or alter citations.
